using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Randomization : MonoBehaviour
{
    public GameObject cloth, cudgel, hair, hands, mirror;
    public GameObject clothDesc, cudgelDesc, hairDesc, handsDesc, mirrorDesc;

    // Essentially, has list of item combinations (coded 1-5 in alphabetical order.)
    // Picks one random element for the red bookmark, and another for the blue bookmark/
    // Assigns items to positions listed (rp1, rp2, rp3 and bp1, bp2, bp3)
    // NOTE: (the following has been set up in the Unity Editor rigidly) Ensure that right item decription sprite is assigned to each icon sprite.
}
